import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Facturacionform3Page } from './facturacionform3.page';

describe('Facturacionform3Page', () => {
  let component: Facturacionform3Page;
  let fixture: ComponentFixture<Facturacionform3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Facturacionform3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
