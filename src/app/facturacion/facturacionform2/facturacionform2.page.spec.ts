import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Facturacionform2Page } from './facturacionform2.page';

describe('Facturacionform2Page', () => {
  let component: Facturacionform2Page;
  let fixture: ComponentFixture<Facturacionform2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Facturacionform2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
